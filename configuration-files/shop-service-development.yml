spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            logger-level: basic
logging:
  level:
    #org.springframework.web.client.RestTemplate: DEBUG
    pl.training: DEBUG

